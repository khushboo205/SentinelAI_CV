# SentinelAI Rules

Never remove an AI agent.

Never change folder structure.

Never rewrite the pipeline.

Only extend functionality.

Every new module must have tests.

Backend:
Python + FastAPI

Frontend:
React + Next.js

Communication:
REST API + WebSocket

Database:
Repository Pattern only

AI:
Agent Based

Every AI component must inherit BaseAgent.